#!/usr/bin/env rake
# encoding: utf-8
require 'bundler/setup'
require 'rainbow'
require 'rspec/core/rake_task'

#--------------------------------------------------------- load external tasks
# tasks in rakelib/*.rake are automatically loaded by rake

#--------------------------------------------------------------------- default
task :default => %w(style spec)
